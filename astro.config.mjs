import { defineConfig } from 'astro/config';

export default defineConfig({
  // Tailwind is handled via PostCSS/Tailwind CLI configuration (postcss.config.cjs)
  // Integration removed to avoid incompatible peer dependency on @astrojs/tailwind
});
